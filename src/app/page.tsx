import { css } from "../../styled-system/css"

export default function Home() {
  return (
    <div
      className={css({
        display: "flex",
        flexDirection: "column",
      })}
    >
      Hello
    </div>
  )
}
